 steps:
  - script: .paket\paket.bootstrapper.exe
    displayName: 'Download paket'

  - script: .\build.ps1 --target Test --Configuration $(buildConfiguration)
    displayName: 'build.ps1 --target Test --Configuration $(buildConfiguration)'